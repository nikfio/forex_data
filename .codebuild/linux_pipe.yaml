version: 0.2

env:
  shell: bash
  git-credential-helper: yes

phases:
  install:
    commands:
      - git log --oneline -10
      - poetry lock --no-update
      - poetry install
    finally:
      - echo "INSTALL phase commands terminated"
  build:
    commands:
      - poetry run pytest
